# 🚀 Инструкция по публикации приложения в интернете

## Варианты размещения

### 0. Запуск по локальной сети (Для тестирования) 🏠
Позволяет запустить приложение на вашем компьютере и получить доступ с других устройств в той же сети (телефон, планшет, другой компьютер).

### 1. Vercel (Рекомендуется для Next.js) ⭐
Vercel создан командой Next.js и идеально подходит для Next.js приложений. Бесплатный план включает:
- Автоматические деплои из GitHub
- HTTPS сертификаты
- CDN
- Переменные окружения
- Превью для каждого Pull Request

### 2. Netlify
Альтернативный вариант с похожими возможностями.

### 3. Railway / Render
Для серверной части (Socket.io сервер для WebRTC).

---

## 🏠 Запуск по локальной сети

### Зачем это нужно?
- Тестирование на мобильных устройствах без деплоя
- Демонстрация приложения другим людям в той же сети
- Отладка на реальных устройствах

### Шаг 1: Узнайте IP-адрес вашего компьютера

#### Windows:
```bash
# Откройте PowerShell или CMD
ipconfig

# Найдите IPv4 адрес (например: 192.168.1.100)
# Обычно это адрес в разделе "Ethernet adapter" или "Wireless LAN adapter"
```

#### macOS/Linux:
```bash
# В терминале
ifconfig

# Или более современная команда
ip addr show

# Найдите адрес вида 192.168.x.x или 10.0.x.x
```

### Шаг 2: Измените настройки сервера

Ваше приложение использует кастомный сервер (`server.js`). Для запуска по локальной сети нужно изменить `hostname`:

1. **Откройте файл `server.js`**

2. **Измените строку:**
   ```javascript
   const hostname = 'localhost'
   ```
   **На:**
   ```javascript
   const hostname = '0.0.0.0'  // Слушать на всех интерфейсах
   ```

3. **Сохраните файл**

### Шаг 3: Запустите приложение

```bash
pnpm dev
```

Приложение запустится и будет доступно по адресу: `http://ВАШ_IP:3000`

Например: `http://192.168.1.100:3000`

### Шаг 4: Настройте Supabase для локальной сети

1. **Откройте Supabase Dashboard** → Ваш проект → **Authentication** → **URL Configuration**

2. **Добавьте в Redirect URLs:**
   ```
   http://ВАШ_IP:3000/**
   http://ВАШ_IP:3000/auth/callback
   ```
   (Замените `ВАШ_IP` на реальный IP-адрес, например: `http://192.168.1.100:3000/**`)

3. **В Site URL** можно оставить `http://localhost:3000` или указать `http://ВАШ_IP:3000`

### Шаг 5: Откройте приложение на других устройствах

На любом устройстве в той же сети (Wi-Fi или Ethernet):
1. Откройте браузер
2. Введите: `http://ВАШ_IP:3000`
3. Приложение должно открыться

### ⚠️ Важные моменты:

1. **Firewall (Брандмауэр):**
   - Windows может заблокировать порт 3000
   - При первом запуске появится запрос на разрешение доступа
   - Или разрешите вручную: Панель управления → Брандмауэр → Разрешить приложение через брандмауэр → Node.js

2. **Все устройства должны быть в одной сети:**
   - Подключены к одному Wi-Fi роутеру
   - Или в одной локальной сети (Ethernet)

3. **Безопасность:**
   - Это только для локальной сети
   - Не доступно из интернета
   - Для доступа из интернета нужен деплой на Vercel/другой сервер

4. **Динамический IP:**
   - IP-адрес может измениться при переподключении к сети
   - Если IP изменился, обновите Redirect URLs в Supabase

### Альтернативный вариант: Использование ngrok (для доступа из интернета)

Если нужно временно дать доступ из интернета:

1. **Установите ngrok:**
   - Скачайте с [ngrok.com](https://ngrok.com)
   - Зарегистрируйтесь (бесплатно)

2. **Запустите приложение:**
   ```bash
   pnpm dev
   ```

3. **В другом терминале запустите ngrok:**
   ```bash
   ngrok http 3000
   ```

4. **Получите публичный URL:**
   - ngrok покажет URL вида: `https://xxxx-xx-xxx-xxx-xx.ngrok-free.app`
   - Этот URL доступен из интернета

5. **Настройте Supabase:**
   - Добавьте ngrok URL в Redirect URLs
   - Обновите Site URL

⚠️ **Важно:** Бесплатная версия ngrok генерирует новый URL при каждом запуске. Для production используйте платный план или деплой на Vercel.

---

## 📋 Подготовка к деплою

### Шаг 1: Подготовка кода

1. **Убедитесь, что код готов к production:**
   ```bash
   # Проверьте сборку
   pnpm build
   
   # Проверьте линтинг
   pnpm lint
   ```

2. **Закоммитьте и запушьте код в GitHub:**
   ```bash
   git add .
   git commit -m "Ready for deployment"
   git push origin main
   ```

### Шаг 2: Настройка Supabase для Production

1. **Откройте Supabase Dashboard** → Ваш проект → **Settings** → **API**

2. **Получите ключи:**
   - `Project URL` (например: `https://xxxxx.supabase.co`)
   - `anon public key`

3. **Настройте Redirect URLs:**
   - Перейдите в **Authentication** → **URL Configuration**
   - Добавьте в **Redirect URLs**:
     ```
     https://your-domain.vercel.app/**
     https://your-domain.vercel.app/auth/callback
     ```
   - В **Site URL** укажите: `https://your-domain.vercel.app`

   ⚠️ **Важно:** Замените `your-domain.vercel.app` на ваш реальный домен после деплоя.

---

## 🚀 Деплой на Vercel (Пошаговая инструкция)

### Вариант А: Через веб-интерфейс (Проще)

1. **Создайте аккаунт на Vercel:**
   - Перейдите на [vercel.com](https://vercel.com)
   - Войдите через GitHub аккаунт

2. **Импортируйте проект:**
   - Нажмите "Add New..." → "Project"
   - Выберите ваш репозиторий GitHub
   - Нажмите "Import"

3. **Настройте проект:**
   - **Framework Preset:** Next.js (автоматически определяется)
   - **Root Directory:** `./` (по умолчанию)
   - **Build Command:** `pnpm build` (или `npm run build`)
   - **Output Directory:** `.next` (по умолчанию)
   - **Install Command:** `pnpm install` (или `npm install`)

4. **Добавьте переменные окружения:**
   - В разделе "Environment Variables" добавьте:
     ```
     NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
     NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
     NEXT_PUBLIC_DEV_SUPABASE_REDIRECT_URL=https://your-domain.vercel.app/lobby
     ```
   - ⚠️ **Важно:** Используйте реальные значения из Supabase!

5. **Деплой:**
   - Нажмите "Deploy"
   - Дождитесь завершения сборки (обычно 2-5 минут)
   - После успешного деплоя получите URL вида: `https://your-project.vercel.app`

6. **Обновите Redirect URLs в Supabase:**
   - Вернитесь в Supabase → **Authentication** → **URL Configuration**
   - Замените `your-domain.vercel.app` на реальный URL из Vercel
   - Добавьте оба URL (с и без `/auth/callback`)

### Вариант Б: Через CLI (Для продвинутых)

1. **Установите Vercel CLI:**
   ```bash
   npm i -g vercel
   ```

2. **Войдите в аккаунт:**
   ```bash
   vercel login
   ```

3. **Деплой:**
   ```bash
   # Первый деплой (вопросы по настройке)
   vercel
   
   # Production деплой
   vercel --prod
   ```

4. **Добавьте переменные окружения через CLI:**
   ```bash
   vercel env add NEXT_PUBLIC_SUPABASE_URL
   vercel env add NEXT_PUBLIC_SUPABASE_ANON_KEY
   vercel env add NEXT_PUBLIC_DEV_SUPABASE_REDIRECT_URL
   ```

---

## 🔧 Настройка WebRTC (Важно!)

Ваше приложение использует Socket.io для WebRTC signaling. **Vercel Serverless Functions не поддерживают WebSockets**, поэтому вам нужно:

### Вариант 1: Отдельный сервер для Socket.io (Рекомендуется)

1. **Создайте отдельный сервер на Railway/Render:**
   - Загрузите файл `server.js` на сервер
   - Настройте переменные окружения
   - Запустите сервер

2. **Обновите код для использования внешнего сервера:**
   - Создайте переменную окружения `NEXT_PUBLIC_SOCKET_SERVER_URL`
   - Обновите код подключения к Socket.io

### Вариант 2: Использовать Vercel Edge Functions (Сложнее)

Требует переписывания Socket.io логики.

### Вариант 3: Временно отключить WebRTC

Если видео не критично, можно временно отключить WebRTC функциональность.

---

## 🌐 Настройка домена (Опционально)

1. **В Vercel Dashboard:**
   - Перейдите в **Settings** → **Domains**
   - Добавьте ваш домен
   - Следуйте инструкциям для настройки DNS

2. **Обновите Redirect URLs в Supabase:**
   - Добавьте новый домен в Redirect URLs
   - Обновите Site URL

---

## 🔒 Безопасность Production

1. **Проверьте RLS политики в Supabase:**
   - Убедитесь, что все политики настроены правильно
   - Не используйте service role key в клиентском коде

2. **Настройте CORS (если нужно):**
   - В Supabase → Settings → API
   - Добавьте ваш домен в разрешенные источники

3. **Ограничения Supabase Free плана:**
   - 500 MB база данных
   - 2 GB bandwidth
   - 50,000 monthly active users
   - Для большего нужен Pro план ($25/месяц)

---

## 🧪 Тестирование после деплоя

1. **Откройте ваш сайт:**
   - Перейдите на URL, который дал Vercel
   - Убедитесь, что сайт загружается

2. **Проверьте базовые функции:**
   - ✅ Регистрация/Вход
   - ✅ Создание игры
   - ✅ Присоединение к игре
   - ✅ Чат (Realtime)
   - ✅ Голосование

3. **Проверьте консоль браузера:**
   - Откройте Developer Tools (F12)
   - Проверьте на ошибки в Console
   - Проверьте Network tab

---

## 🐛 Решение проблем

### Ошибка: "Failed to load game state"
- Проверьте переменные окружения в Vercel
- Убедитесь, что ключи Supabase правильные

### Ошибка: "Not authenticated"
- Проверьте Redirect URLs в Supabase
- Убедитесь, что Site URL правильный

### WebRTC не работает
- Socket.io требует отдельный сервер (не работает на Serverless)
- Рассмотрите вариант с Railway/Render для Socket.io сервера

### Realtime не работает
- Проверьте, что Realtime включен в Supabase
- Проверьте Network tab на ошибки подключения

---

## 📊 Мониторинг

1. **Vercel Analytics:**
   - Включите Analytics в Vercel Dashboard
   - Отслеживайте производительность

2. **Supabase Dashboard:**
   - Мониторьте использование базы данных
   - Проверяйте логи API

3. **Ошибки:**
   - Vercel показывает логи деплоя
   - Supabase показывает логи базы данных

---

## 💰 Стоимость

### Бесплатный вариант (для начала):
- **Vercel:** Бесплатно (до 100GB bandwidth)
- **Supabase:** Бесплатно (500 MB база, 2 GB bandwidth)
- **Итого:** $0/месяц

### Для более серьезного использования:
- **Vercel Pro:** $20/месяц
- **Supabase Pro:** $25/месяц
- **Railway (для Socket.io):** ~$5-10/месяц
- **Итого:** ~$50-55/месяц

---

## 📝 Чек-лист перед деплоем

- [ ] Код закоммичен и запушен в GitHub
- [ ] Сборка проходит без ошибок (`pnpm build`)
- [ ] Получены ключи из Supabase
- [ ] Создан аккаунт на Vercel
- [ ] Настроены переменные окружения
- [ ] Настроены Redirect URLs в Supabase
- [ ] Проверена работа приложения локально
- [ ] (Опционально) Настроен отдельный сервер для Socket.io
- [ ] (Опционально) Настроен домен

---

## 🎉 Готово!

После успешного деплоя ваше приложение будет доступно в интернете!

**Следующие шаги:**
1. Поделитесь ссылкой с друзьями
2. Соберите обратную связь
3. Мониторьте использование
4. Масштабируйте при необходимости

---

**Нужна помощь?**
- [Документация Vercel](https://vercel.com/docs)
- [Документация Supabase](https://supabase.com/docs)
- [Документация Next.js](https://nextjs.org/docs)
