# 🚀 Инструкция по запуску приложения "Бункер Онлайн"

## Быстрый запуск

### Шаг 1: Установка зависимостей

Если зависимости еще не установлены, выполните:

```bash
pnpm install
```

**Примечание:** Если у вас не установлен `pnpm`, установите его командой:
```bash
npm install -g pnpm
```

### Шаг 2: Запуск приложения

Для запуска приложения в режиме разработки выполните:

**Вариант 1: Через pnpm (рекомендуется)**
```bash
pnpm dev
```

**Вариант 2: Через batch-файл (если проблемы с кириллицей в пути)**
Просто дважды кликните по файлу `start.bat` в корне проекта.

**Вариант 3: Напрямую через node**
```bash
node server.js
```

После запуска подождите 5-10 секунд для компиляции Next.js, затем откройте в браузере: **http://localhost:3000**

---

## Подробная инструкция

### Требования

- **Node.js** версии 18 или выше (рекомендуется 20+)
- **pnpm** - менеджер пакетов ([установка](https://pnpm.io/installation))
- **Supabase аккаунт** (для полноценной работы приложения)

### Установка зависимостей

Перейдите в папку проекта и установите зависимости:

```bash
cd bunker-online-app
pnpm install
```

Это установит все необходимые пакеты из `package.json`.

### Настройка переменных окружения (опционально)

Для полноценной работы приложения (подключение к базе данных, аутентификация) создайте файл `.env.local` в корне проекта:

```env
NEXT_PUBLIC_SUPABASE_URL=ваш_url_из_supabase
NEXT_PUBLIC_SUPABASE_ANON_KEY=ваш_ключ_из_supabase
```

**Где взять ключи:**
1. Зайдите на [supabase.com](https://supabase.com)
2. Создайте проект или откройте существующий
3. Перейдите: **Settings** → **API**
4. Скопируйте:
   - **Project URL** → вставьте в `NEXT_PUBLIC_SUPABASE_URL`
   - **anon public key** → вставьте в `NEXT_PUBLIC_SUPABASE_ANON_KEY`

**Примечание:** Приложение запустится и без `.env.local`, но функции, требующие подключения к базе данных, работать не будут.

### Запуск приложения

#### Режим разработки (Development)

```bash
pnpm dev
```

Эта команда запускает кастомный сервер Next.js с поддержкой Socket.io для WebRTC сигнализации.

Приложение будет доступно по адресу: **http://localhost:3000**

#### Режим production

Для сборки и запуска production версии:

```bash
# Сборка приложения
pnpm build

# Запуск production сервера
pnpm start
```

### Другие полезные команды

```bash
# Проверка кода (линтинг)
pnpm lint

# Запуск только Next.js dev сервера (без Socket.io)
pnpm dev:next
```

---

## Проверка работы

После запуска:

1. Откройте браузер и перейдите на http://localhost:3000
2. Если видите главную страницу - приложение работает! ✅
3. Для полноценной работы:
   - Настройте Supabase (см. `README.md` для подробностей)
   - Создайте базу данных (выполните SQL скрипты из папки `scripts/`)
   - Настройте Realtime в Supabase

---

## Остановка приложения

Для остановки сервера нажмите в терминале:

- **Windows/Linux/Mac:** `Ctrl + C`

---

## Решение проблем

### Порт 3000 уже занят

Если порт 3000 уже используется другим приложением:

1. Найти процесс, использующий порт:
   ```bash
   # Windows
   netstat -ano | findstr :3000
   
   # Linux/Mac
   lsof -i :3000
   ```

2. Закрыть процесс или использовать другой порт:
   ```bash
   # Установить переменную окружения перед запуском
   set PORT=3001
   pnpm dev
   ```

### Ошибка "Cannot find module"

Переустановите зависимости:

```bash
rm -rf node_modules
pnpm install
```

### Ошибки при сборке TypeScript

Проверьте ошибки:
```bash
pnpm lint
```

### Проблема с кириллицей в пути к проекту

Если путь к проекту содержит кириллические символы (например, `c:\общая\bunker-online-app`), могут возникнуть проблемы с запуском через PowerShell.

**Решение 1: Используйте batch-файл для запуска**

В корне проекта создан файл `start.bat`. Просто дважды кликните по нему или запустите из командной строки:
```cmd
start.bat
```

**Решение 2: Используйте cmd вместо PowerShell**

Откройте обычную командную строку (cmd) и выполните:
```cmd
cd c:\общая\bunker-online-app
pnpm dev
```

**Решение 3: Переместите проект в папку с английским названием**

Самый надежный способ - переместить проект в папку с латинскими символами, например:
```cmd
C:\projects\bunker-online-app
```

### Приложение не открывается в браузере

Если порт 3000 занят, но страница не открывается:

1. **Проверьте, что сервер действительно запущен:**
   ```cmd
   netstat -ano | findstr :3000
   ```

2. **Попробуйте открыть в браузере вручную:**
   - Откройте браузер
   - Введите в адресной строке: `http://localhost:3000`
   - Или: `http://127.0.0.1:3000`

3. **Если страница не загружается:**
   - Подождите 10-20 секунд (Next.js компилируется при первом запуске)
   - Проверьте консоль терминала на наличие ошибок
   - Убедитесь, что файрвол не блокирует соединение

4. **Перезапустите сервер:**
   ```cmd
   # Остановите текущий процесс (Ctrl+C)
   # Затем запустите снова:
   pnpm dev
   ```

---

## Что делает команда `pnpm dev`?

Команда `pnpm dev` запускает файл `server.js`, который:

1. Запускает Next.js приложение в режиме разработки
2. Инициализирует Socket.io сервер для WebRTC сигнализации
3. Обеспечивает обработку WebSocket соединений для видеосвязи между игроками

---

## Дополнительная документация

- **Полная инструкция:** см. `README.md`
- **Настройка перед запуском:** см. `НАСТРОЙКА_ПЕРЕД_ЗАПУСКОМ.md`
- **Архитектура проекта:** см. `АРХИТЕКТУРА.md`

---

**Удачи в разработке! 🎮**
