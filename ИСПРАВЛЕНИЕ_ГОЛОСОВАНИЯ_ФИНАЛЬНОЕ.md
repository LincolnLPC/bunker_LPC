# 🔧 Исправление проблемы с голосованием - ФИНАЛЬНОЕ РЕШЕНИЕ

## Проблема
При попытке проголосовать возникает ошибка:
```
new row violates row-level security policy for table "votes"
```

## Решение (2 варианта)

### Вариант 1: Использование Service Role Key (РЕКОМЕНДУЕТСЯ)

Это самый простой способ - API будет использовать service role key для обхода RLS при голосовании.

**Шаги:**

1. **Получите Service Role Key:**
   - Откройте Supabase Dashboard: https://supabase.com/dashboard
   - Выберите ваш проект
   - Перейдите: **Settings → API**
   - Найдите секцию **Project API keys**
   - Скопируйте **service_role** key (начинается с `eyJ...` или `sb_secret_...`)

2. **Добавьте ключ в .env.local:**
   - Откройте файл `.env.local` в корне проекта
   - Добавьте строку:
     ```env
     SUPABASE_SERVICE_ROLE_KEY=ваш_service_role_ключ_здесь
     ```
   - **ВАЖНО:** Никогда не коммитьте этот ключ в Git! Он дает полный доступ к базе данных.

3. **Перезапустите сервер разработки:**
   ```bash
   # Остановите сервер (Ctrl+C)
   # Запустите снова
   pnpm dev
   ```

4. **Проверьте работу:**
   - Перезагрузите страницу приложения
   - Попробуйте проголосовать
   - Ошибка должна исчезнуть

### Вариант 2: Исправление RLS политики

Если вы не хотите использовать service role key, выполните SQL скрипт:

1. **Откройте Supabase Dashboard → SQL Editor**

2. **Выполните скрипт из файла `scripts/016-fix-votes-rls-policy-simple.sql`:**

   ```sql
   -- Drop existing policies
   DROP POLICY IF EXISTS "Players can cast votes" ON votes;
   DROP POLICY IF EXISTS "Players can update votes" ON votes;

   -- Create simple INSERT policy
   CREATE POLICY "Players can cast votes" 
   ON votes 
   FOR INSERT 
   WITH CHECK (
     -- Check that voter belongs to current user and is in the room
     EXISTS (
       SELECT 1 
       FROM game_players gp
       WHERE gp.id = voter_id 
         AND gp.user_id = auth.uid()
         AND gp.room_id = room_id
         AND gp.is_eliminated = FALSE
     )
     -- Check that target is in the same room
     AND EXISTS (
       SELECT 1 
       FROM game_players gp
       WHERE gp.id = target_id
         AND gp.room_id = room_id
         AND gp.is_eliminated = FALSE
     )
   );

   -- Create UPDATE policy for upsert operations
   CREATE POLICY "Players can update votes" 
   ON votes 
   FOR UPDATE 
   USING (
     -- Can update if voter belongs to current user
     EXISTS (
       SELECT 1 
       FROM game_players gp
       WHERE gp.id = voter_id 
         AND gp.user_id = auth.uid()
         AND gp.room_id = room_id
         AND gp.is_eliminated = FALSE
     )
   )
   WITH CHECK (
     -- After update, same checks as INSERT
     EXISTS (
       SELECT 1 
       FROM game_players gp
       WHERE gp.id = voter_id 
         AND gp.user_id = auth.uid()
         AND gp.room_id = room_id
         AND gp.is_eliminated = FALSE
     )
     AND EXISTS (
       SELECT 1 
       FROM game_players gp
       WHERE gp.id = target_id
         AND gp.room_id = room_id
         AND gp.is_eliminated = FALSE
     )
   );
   ```

3. **Нажмите RUN**

4. **Проверьте результат:**
   - После выполнения вы должны увидеть: `Success. No rows returned`
   - Перезагрузите страницу приложения
   - Попробуйте проголосовать

## Почему это работает?

### Вариант 1 (Service Role):
- API использует service role key, который обходит RLS
- Все проверки безопасности выполняются в коде API перед записью
- Это безопасно, так как API проверяет:
  - Пользователь авторизован
  - Пользователь является игроком в комнате
  - Пользователь не исключен
  - Цель существует и не исключена
  - Комната в фазе голосования

### Вариант 2 (RLS Policy):
- RLS политика проверяет те же условия на уровне базы данных
- Более безопасно, но требует правильной настройки политики

## Рекомендация

**Используйте Вариант 1 (Service Role Key)** - это проще и надежнее. API уже выполняет все необходимые проверки безопасности, поэтому использование service role key безопасно.

## Если проблема осталась

1. Убедитесь, что `.env.local` содержит правильные ключи
2. Перезапустите сервер разработки
3. Очистите кеш браузера (Ctrl+Shift+R)
4. Проверьте консоль браузера на наличие других ошибок
