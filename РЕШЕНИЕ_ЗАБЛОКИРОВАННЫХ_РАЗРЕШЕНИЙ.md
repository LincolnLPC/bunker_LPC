# 🔒 Решение проблемы: Разрешения заблокированы

## Проблема

При нажатии на кнопку "Включить камеру" запрос на разрешение не появляется. Это означает, что разрешения были **заблокированы** (denied) в настройках браузера.

Когда разрешения заблокированы, браузер **не показывает диалог** - он сразу возвращает ошибку `NotAllowedError: Permission denied`.

## 🔍 Как проверить статус разрешений

### В консоли браузера (F12):

```javascript
// Проверить статус разрешений
navigator.permissions.query({ name: 'camera' }).then(result => {
  console.log('Camera permission:', result.state) // 'granted', 'denied', or 'prompt'
})

navigator.permissions.query({ name: 'microphone' }).then(result => {
  console.log('Microphone permission:', result.state) // 'granted', 'denied', or 'prompt'
})
```

**Возможные значения:**
- `'granted'` - разрешено ✅
- `'prompt'` - будет запрошено при следующем вызове ✅
- `'denied'` - заблокировано ❌ (нужно сбросить)

## ✅ Решение: Сброс заблокированных разрешений

### Chrome / Edge:

1. **Способ 1: Через значок замка в адресной строке**
   - Нажмите на значок **🔒** (замок) слева от адреса `http://localhost:3000`
   - Найдите раздел **"Камера"** и **"Микрофон"**
   - Если установлено **"Заблокировано"**, измените на **"Разрешить"**
   - Обновите страницу (F5)

2. **Способ 2: Через настройки браузера**
   - Откройте `chrome://settings/content/camera` (или `edge://settings/content/camera`)
   - Найдите `http://localhost:3000` в списке сайтов
   - Удалите его из списка или измените на "Разрешить"
   - Обновите страницу

3. **Способ 3: Полный сброс**
   - Откройте `chrome://settings/content/camera`
   - Нажмите "Очистить данные" или удалите `localhost:3000` из списка
   - Откройте `chrome://settings/content/microphone`
   - Нажмите "Очистить данные" или удалите `localhost:3000` из списка
   - Обновите страницу
   - При следующем запросе появится диалог

### Firefox:

1. **Через значок замка:**
   - Нажмите на значок **🔒** в адресной строке
   - Нажмите **"Больше информации"**
   - Перейдите на вкладку **"Разрешения"**
   - Найдите **"Камера"** и **"Микрофон"**
   - Если установлено **"Заблокировано"**, измените на **"Разрешить"**
   - Обновите страницу

2. **Через настройки:**
   - Откройте `about:preferences#privacy`
   - Найдите раздел **"Разрешения"**
   - Нажмите **"Настройки"** рядом с "Камера" и "Микрофон"
   - Найдите `localhost:3000` и удалите или измените на "Разрешить"
   - Обновите страницу

### Safari (macOS):

1. Safari → **Настройки** → **Веб-сайты** → **Камера**
2. Найдите `localhost` или ваш домен
3. Измените с **"Запретить"** на **"Спросить"** или **"Разрешить"**
4. Повторите для **Микрофон**
5. Обновите страницу

## 🧪 Тестирование после сброса

После сброса разрешений:

1. Обновите страницу (F5)
2. Нажмите кнопку **"Включить камеру"**
3. Должно появиться диалоговое окно браузера с запросом разрешения
4. Нажмите **"Разрешить"**

## 📋 Пошаговая инструкция для Chrome/Edge

1. **Откройте настройки разрешений:**
   ```
   chrome://settings/content/camera
   chrome://settings/content/microphone
   ```

2. **Найдите localhost:3000 в списке:**
   - Если есть - удалите или измените на "Разрешить"
   - Если нет - это нормально, разрешения будут запрошены при следующем вызове

3. **Вернитесь на страницу игры**

4. **Обновите страницу** (F5 или Ctrl+R)

5. **Нажмите кнопку "Включить камеру"**

6. **Должно появиться диалоговое окно** с запросом разрешения

7. **Нажмите "Разрешить"**

## ⚠️ Важно

- Если разрешения **заблокированы** (denied), браузер **не показывает диалог** - он сразу возвращает ошибку
- Это сделано для безопасности - чтобы сайты не могли постоянно запрашивать доступ
- После сброса разрешений диалог появится снова

## 🔍 Диагностика в консоли

Выполните в консоли браузера (F12):

```javascript
// Проверить статус
Promise.all([
  navigator.permissions.query({ name: 'camera' }),
  navigator.permissions.query({ name: 'microphone' })
]).then(([camera, mic]) => {
  console.log('Camera:', camera.state)
  console.log('Microphone:', mic.state)
  
  if (camera.state === 'denied' || mic.state === 'denied') {
    console.warn('⚠️ Разрешения заблокированы! Нужно сбросить в настройках браузера.')
  } else {
    console.log('✅ Разрешения не заблокированы')
  }
})
```

## 💡 Альтернативное решение

Если не удается сбросить разрешения:

1. Используйте **режим инкогнито** (Ctrl+Shift+N)
2. Или используйте **другой браузер**
3. Или используйте **другой порт** (например, `localhost:3001`)

---

**После сброса разрешений кнопка "Включить камеру" должна показать диалог запроса разрешения!**
