# 🚀 Быстрый старт: Логирование и мониторинг

## Запуск сервера с логированием

### Терминал 1: Запуск сервера
```bash
pnpm dev:log
```

Сервер запустится и все логи будут автоматически сохраняться в файл:
- 📁 Директория: `logs/`
- 📄 Формат: `server-YYYY-MM-DDTHH-MM-SS.log`

### Терминал 2: Мониторинг логов (опционально)
```bash
# Мониторить последний файл логов в реальном времени
pnpm monitor:log

# Показать все логи (не только важные)
pnpm monitor:log --all
```

## Что будет в логах

✅ **Автоматически сохраняется:**
- Все логи сервера (stdout)
- Все ошибки (stderr)
- Socket.io события
- WebRTC signaling
- Peer connection события

✅ **Автоматически фильтруется при мониторинге:**
- Socket.io события (синий)
- WebRTC события (фиолетовый)
- PeerConnection события (фиолетовый)
- Сигналы (offer/answer/ICE) (голубой)
- Ошибки (красный)
- Предупреждения (желтый)

## Пример использования

1. **Терминал 1**: `pnpm dev:log` - запуск сервера
2. **Терминал 2**: `pnpm monitor:log` - мониторинг логов
3. **Браузер**: Откройте приложение и проверьте работу

Все события будут видны в терминале 2 в реальном времени!

## Просмотр старых логов

```bash
# Просмотреть конкретный файл
node scripts/monitor-log-file.js logs/server-2024-01-11T14-30-15.log

# Или просто откройте файл в текстовом редакторе
```

## Остановка

- **Сервер**: `Ctrl+C` в терминале 1
- **Мониторинг**: `Ctrl+C` в терминале 2

Логи останутся сохраненными в файлах для последующего анализа!
