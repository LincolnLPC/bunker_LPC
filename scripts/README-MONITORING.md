# Скрипты для мониторинга WebRTC Signaling и логов

## 🆕 Сохранение логов в файл (Рекомендуется)

### 1. Запуск сервера с сохранением логов

```bash
# Запустить сервер с автоматическим сохранением логов
node scripts/log-server.js

# Или через npm
pnpm dev:log
```

Логи будут сохранены в директорию `logs/` с именем файла, содержащим дату и время запуска.

### 2. Мониторинг логов из файла

В **отдельном терминале** запустите:

```bash
# Мониторить последний файл логов
node scripts/monitor-log-file.js

# Или через npm
pnpm monitor:log

# Мониторить конкретный файл
node scripts/monitor-log-file.js logs/server-2024-01-11T14-30-15.log

# Показать все логи (не только важные)
node scripts/monitor-log-file.js --all
```

**Преимущества:**
- ✅ Логи сохраняются в файл для последующего анализа
- ✅ Можно мониторить логи в реальном времени
- ✅ Автоматическая фильтрация важных событий
- ✅ Статистика каждые 30 секунд

---

## Скрипты для мониторинга WebRTC Signaling

## 1. Мониторинг Signaling (Рекомендуется)

Мониторит WebRTC сигналы через Socket.io в реальном времени.

### Использование:

```bash
# Мониторинг конкретной комнаты
node scripts/monitor-signaling.js <roomId>

# Или через npm
pnpm monitor <roomId>
```

### Пример:

```bash
pnpm monitor 40dd7fba-e304-4ca7-95e3-aa92bb41e57a
```

### Что показывает:

- ✅ Подключения/отключения клиентов
- 📡 Присоединения к комнатам
- 📨 Все WebRTC сигналы (offers, answers, ICE candidates)
- 📊 Статистику каждые 10 секунд
- ❌ Ошибки подключения

### Пример вывода:

```
🔍 WebRTC Signaling Monitor
============================================================
Server: http://localhost:3000
Room ID: 40dd7fba-e304-4ca7-95e3-aa92bb41e57a
============================================================

[14:30:15.123] [CONNECT] ✅ Connected to server (Socket ID: abc123)
[14:30:15.456] [ROOM] 📡 Joining room: 40dd7fba-e304-4ca7-95e3-aa92bb41e57a
[14:30:15.789] [ROOM] ✅ Joined room: 40dd7fba-e304-4ca7-95e3-aa92bb41e57a
[14:30:16.012] [SIGNAL] OFFER from player1 → player2
[14:30:16.234] [SIGNAL] ANSWER from player2 → player1
[14:30:16.456] [SIGNAL] ICE-CANDIDATE from player1 → player2
```

---

## 2. Мониторинг логов сервера

Фильтрует и форматирует логи сервера (Socket.io, WebRTC).

### Использование:

```bash
# Запустить сервер с перенаправлением логов
pnpm dev 2>&1 | node scripts/monitor-server-logs.js

# Или через npm
pnpm dev 2>&1 | pnpm monitor:server
```

### Что показывает:

- Только важные логи (Socket.io, WebRTC, PeerConnection)
- Цветовое форматирование
- Статистику каждые 30 секунд
- Фильтрацию по типам событий

---

## Советы по использованию

1. **Для отладки WebRTC**: Используйте `monitor-signaling.js` с roomId
2. **Для общего мониторинга**: Используйте `monitor-server-logs.js`
3. **Запустите в отдельном терминале**: Чтобы видеть логи в реальном времени
4. **Остановка**: Нажмите `Ctrl+C`

---

## Получение Room ID

Room ID можно найти:
- В URL браузера: `/game/[roomCode]` → roomCode нужно преобразовать в roomId
- В консоли браузера: `[WebRTC] 🔌 Initializing signaling for room: <roomId>`
- В логах сервера: `[SocketIO Server] 📡 Player ... joining room <roomId>`

---

## Пример полной отладки

1. **Терминал 1**: Запустить сервер
   ```bash
   pnpm dev
   ```

2. **Терминал 2**: Запустить мониторинг signaling
   ```bash
   pnpm monitor <roomId>
   ```

3. **Браузер**: Открыть консоль (F12) и смотреть логи клиента

Теперь вы видите:
- Логи сервера (терминал 1)
- Signaling события (терминал 2)
- Логи клиента (браузер)
