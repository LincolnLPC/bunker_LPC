# ⚠️ Настройка перед запуском

## Текущий статус

✅ Зависимости установлены  
⚠️ Файл `.env.local` не найден  
⚠️ Приложение запущено, но без Supabase не будет работать

## Следующие шаги

### 1. Создайте файл `.env.local`

В корне проекта создайте файл `.env.local` со следующим содержимым:

```env
NEXT_PUBLIC_SUPABASE_URL=ваш_url_из_supabase
NEXT_PUBLIC_SUPABASE_ANON_KEY=ваш_ключ_из_supabase
```

### 2. Получите ключи из Supabase

1. Зайдите на https://supabase.com
2. Создайте проект (или используйте существующий)
3. Перейдите: **Settings** → **API**
4. Скопируйте:
   - **Project URL** → вставьте в `NEXT_PUBLIC_SUPABASE_URL`
   - **anon public key** → вставьте в `NEXT_PUBLIC_SUPABASE_ANON_KEY`

### 3. Создайте базу данных

1. В Supabase перейдите: **SQL Editor**
2. Откройте файл `scripts/001-create-tables.sql` из проекта
3. Скопируйте весь SQL код
4. Вставьте в SQL Editor и нажмите **Run**

### 4. Включите Realtime

1. Перейдите: **Database** → **Replication**
2. Включите для таблиц:
   - `game_rooms`
   - `game_players`
   - `chat_messages`
   - `player_characteristics`
   - `votes`

### 5. Перезапустите сервер

```bash
# Остановите текущий процесс (Ctrl+C)
# Затем запустите снова:
pnpm dev
```

## Быстрая проверка

После настройки откройте http://localhost:3000

Если видите главную страницу - все работает! 🎉

## Проблема с путем (кириллица)

Если возникают проблемы из-за кириллицы в пути к проекту, попробуйте:
- Переместить проект в папку с английским названием
- Или использовать короткий путь через символическую ссылку

---

Подробная инструкция: см. `README.md`
