# 🔍 Решение проблем с камерой и микрофоном

## Почему приложение не находит камеру и микрофон?

### Основные причины:

1. **Браузер не поддерживает WebRTC API**
   - Старые версии браузеров
   - Браузеры без поддержки (некоторые мобильные)

2. **Разрешения не предоставлены**
   - Пользователь отклонил запрос на доступ
   - Браузер блокирует доступ по умолчанию

3. **Устройства используются другим приложением**
   - Другое приложение (Zoom, Teams, Skype) использует камеру/микрофон
   - Другая вкладка браузера использует устройства

4. **Настройки приватности браузера**
   - Блокировка доступа к медиа в настройках
   - Режим инкогнито (может ограничивать доступ)

5. **Проблемы с драйверами**
   - Драйверы камеры/микрофона не установлены или устарели
   - Устройства не распознаются системой

---

## 🔧 Как проверить и исправить

### 1. Проверьте поддержку браузера

Откройте консоль браузера (F12) и выполните:

```javascript
// Проверка поддержки getUserMedia
console.log("mediaDevices:", !!navigator.mediaDevices)
console.log("getUserMedia:", !!navigator.mediaDevices?.getUserMedia)

// Должно вывести: true true
```

**Если выводит `false`:**
- Обновите браузер до последней версии
- Используйте современный браузер: Chrome, Firefox, Edge, Safari

### 2. Проверьте разрешения в браузере

#### Chrome/Edge:
1. Откройте `chrome://settings/content/camera` (или `edge://settings/content/camera`)
2. Убедитесь, что доступ разрешен
3. Проверьте список сайтов - разрешите доступ для `http://localhost:3000`

#### Firefox:
1. Откройте `about:preferences#privacy`
2. Разрешите доступ к камере и микрофону
3. В настройках сайта разрешите доступ для localhost

#### Safari:
1. Safari → Настройки → Веб-сайты → Камера/Микрофон
2. Разрешите доступ для localhost

### 3. Проверьте, используются ли устройства другим приложением

**Windows:**
1. Откройте Диспетчер задач (Ctrl+Shift+Esc)
2. Проверьте, какие приложения используют камеру/микрофон
3. Закройте другие приложения, которые могут использовать устройства

**macOS:**
1. Системные настройки → Конфиденциальность и безопасность → Камера/Микрофон
2. Проверьте список приложений
3. Закройте приложения, которые используют устройства

### 4. Проверьте доступ к устройствам в системе

**Windows:**
1. Параметры → Конфиденциальность → Камера/Микрофон
2. Убедитесь, что доступ включен
3. Проверьте, что ваше приложение/браузер в списке разрешенных

**macOS:**
1. Системные настройки → Конфиденциальность и безопасность → Камера/Микрофон
2. Разрешите доступ для вашего браузера

### 5. Проверьте доступ через HTTPS (для production)

⚠️ **Важно:** В production (не localhost) требуется HTTPS для доступа к камере/микрофону.

**Для localhost это не требуется**, но если используете IP-адрес (например, `http://192.168.1.100:3000`), может потребоваться HTTPS.

### 6. Проверьте консоль браузера на ошибки

Откройте консоль (F12) и проверьте:

- **Если видите:** `[WebRTC] Browser doesn't support getUserMedia`
  → Браузер не поддерживает API
  
- **Если видите:** `NotAllowedError` или `Permission denied`
  → Разрешения не предоставлены

- **Если видите:** `NotFoundError` или `DevicesNotFoundError`
  → Устройства не найдены или не подключены

- **Если видите:** `NotReadableError` или `TrackStartError`
  → Устройства используются другим приложением

---

## 🧪 Тестирование доступа к медиа

Выполните в консоли браузера (F12):

```javascript
// Тест доступа к камере и микрофону
navigator.mediaDevices.getUserMedia({ video: true, audio: true })
  .then(stream => {
    console.log("✅ Доступ получен!", stream)
    console.log("Видео треки:", stream.getVideoTracks())
    console.log("Аудио треки:", stream.getAudioTracks())
    
    // Остановить поток после теста
    stream.getTracks().forEach(track => track.stop())
  })
  .catch(err => {
    console.error("❌ Ошибка:", err.name, err.message)
    
    if (err.name === 'NotAllowedError') {
      console.error("Разрешения не предоставлены")
    } else if (err.name === 'NotFoundError') {
      console.error("Устройства не найдены")
    } else if (err.name === 'NotReadableError') {
      console.error("Устройства используются другим приложением")
    }
  })
```

---

## ✅ Пошаговая диагностика

1. **Проверьте консоль браузера:**
   - Откройте Developer Tools (F12)
   - Перейдите на вкладку Console
   - Посмотрите на сообщения с префиксом `[WebRTC]` или `[Media]`

2. **Проверьте доступ к устройствам:**
   - Убедитесь, что камера и микрофон подключены
   - Проверьте, что они работают в других приложениях (например, Zoom, Skype)

3. **Проверьте разрешения браузера:**
   - Откройте настройки браузера
   - Найдите раздел "Разрешения сайта" или "Конфиденциальность"
   - Проверьте настройки камеры и микрофона

4. **Попробуйте в другом браузере:**
   - Chrome
   - Firefox
   - Edge
   - Safari (на macOS)

5. **Попробуйте на другом устройстве:**
   - Если работает на другом устройстве - проблема в настройках первого
   - Если не работает нигде - проблема в коде

---

## 🎯 Быстрое решение

### Вариант 1: Использовать кнопку "Включить камеру"

Если автоматический запрос не работает:
1. Игра запустится без видео/аудио
2. Внизу экрана будет кнопка "Включить камеру"
3. Нажмите на неё - появится запрос на разрешение
4. Разрешите доступ

### Вариант 2: Очистить разрешения и попробовать снова

1. Откройте настройки браузера
2. Найдите список сайтов с доступом к камере/микрофону
3. Удалите `localhost:3000` из списка
4. Обновите страницу
5. При повторном запросе разрешите доступ

### Вариант 3: Использовать другой браузер

Если текущий браузер не работает, попробуйте:
- **Chrome** (рекомендуется) - лучшая поддержка WebRTC
- **Firefox** - хорошая поддержка
- **Edge** - основан на Chrome, должна работать

---

## 📱 Мобильные устройства

На мобильных устройствах могут быть дополнительные ограничения:

1. **iOS (Safari):**
   - Требуется HTTPS (кроме localhost)
   - Разрешения запрашиваются автоматически
   - Может потребоваться явное взаимодействие пользователя

2. **Android (Chrome):**
   - Обычно работает как на десктопе
   - Проверьте настройки разрешений в настройках устройства

---

## 🔍 Дополнительная диагностика

### Проверить доступные устройства:

Выполните в консоли:

```javascript
// Список всех доступных устройств
navigator.mediaDevices.enumerateDevices()
  .then(devices => {
    console.log("Доступные устройства:")
    devices.forEach(device => {
      console.log(`${device.kind}: ${device.label} (${device.deviceId})`)
    })
  })
```

Если список пустой или устройства без названий (`label` пустой) - возможно, доступ к устройствам заблокирован.

---

## 💡 Частые проблемы и решения

### Проблема: "Browser doesn't support getUserMedia"
**Решение:** Обновите браузер или используйте современный браузер

### Проблема: "Permission denied" / "NotAllowedError"
**Решение:** 
1. Разрешите доступ в настройках браузера
2. Очистите разрешения и попробуйте снова
3. Проверьте, не блокирует ли расширение браузера доступ

### Проблема: "NotFoundError" / "DevicesNotFoundError"
**Решение:**
1. Убедитесь, что устройства подключены
2. Проверьте драйверы
3. Попробуйте переподключить устройства

### Проблема: "NotReadableError" / "TrackStartError"
**Решение:**
1. Закройте другие приложения, использующие камеру/микрофон
2. Перезагрузите страницу
3. Перезагрузите компьютер

---

## 🎮 Игра без видео/аудио

**Важно:** Игра может работать без видеосвязи!

Если не удается решить проблему с медиа:
- Игра продолжит работать
- Можно общаться через чат
- Голосование и другие функции работают нормально
- Видеосвязь необязательна для игры

---

## 📞 Если ничего не помогло

1. Проверьте логи в консоли браузера
2. Проверьте логи сервера (если есть)
3. Попробуйте на другом устройстве/браузере
4. Проверьте, работает ли WebRTC на других сайтах (например, https://webrtc.github.io/samples/)

---

**Нужна помощь?** Опишите ошибку из консоли браузера, и я помогу найти решение!
