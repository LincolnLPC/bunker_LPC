# Бункер Онлайн - Инструкция по запуску

Веб-приложение для игры "Бункер" с видеосвязью и синхронизацией в реальном времени.

## 📋 Требования

- **Node.js** 18+ (рекомендуется 20+)
- **pnpm** (менеджер пакетов) - [установка](https://pnpm.io/installation)
- **Supabase аккаунт** (бесплатный) - [supabase.com](https://supabase.com)

## 🚀 Быстрый старт

### 1. Клонирование и установка зависимостей

```bash
# Клонируйте репозиторий (если еще не сделано)
cd bunker-online-app

# Установите зависимости
pnpm install
```

### 2. Настройка Supabase

#### 2.1. Создайте проект в Supabase

1. Перейдите на [supabase.com](https://supabase.com)
2. Войдите в аккаунт или зарегистрируйтесь
3. Нажмите "New Project"
4. Заполните данные:
   - **Name**: `bunker-online` (или любое другое)
   - **Database Password**: придумайте надежный пароль (сохраните его!)
   - **Region**: выберите ближайший регион
   - **Pricing Plan**: Free (достаточно для разработки)

#### 2.2. Создайте базу данных

1. В панели Supabase перейдите в **SQL Editor**
2. Создайте новый запрос
3. Скопируйте содержимое файла `scripts/001-create-tables.sql`
4. Вставьте в SQL Editor и выполните (Run)

#### 2.3. Получите API ключи

1. В панели Supabase перейдите в **Settings** → **API**
2. Скопируйте следующие значения:
   - **Project URL** (например: `https://xxxxx.supabase.co`)
   - **anon public key** (длинная строка начинающаяся с `eyJ...`)
   - **service_role key** (для админ операций - НЕ публикуйте в клиентском коде!)

### 3. Настройка переменных окружения

Создайте файл `.env.local` в корне проекта:

```bash
# .env.local

# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=your_project_url_here
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key_here

# Supabase Service Role Key (required for admin operations)
# Get this from Supabase Dashboard → Settings → API → service_role key
# WARNING: Never expose this key in client-side code!
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# Optional: For development redirect after email confirmation
NEXT_PUBLIC_DEV_SUPABASE_REDIRECT_URL=http://localhost:3000/lobby
```

**Важно**: Замените `your_project_url_here` и `your_anon_key_here` на реальные значения из Supabase.

### 4. Настройка аутентификации в Supabase

1. В панели Supabase перейдите в **Authentication** → **URL Configuration**
2. Добавьте в **Redirect URLs**:
   - `http://localhost:3000/**` (для разработки)
   - `http://localhost:3000/auth/callback` (для обработки редиректов)
3. В **Site URL** укажите: `http://localhost:3000`

### 5. Включение Realtime в Supabase

1. Перейдите в **Database** → **Replication**
2. Включите репликацию для следующих таблиц:
   - `game_rooms`
   - `game_players`
   - `chat_messages`
   - `player_characteristics`
   - `votes`

   (Нажмите на тумблер рядом с каждой таблицей)

### 6. Настройка Storage (опционально, для аватаров)

1. Перейдите в **Storage**
2. Создайте новый bucket:
   - **Name**: `avatars`
   - **Public bucket**: включите
3. В **Policies** добавьте политику:
   - **Policy name**: `Public Avatar Access`
   - **Allowed operation**: SELECT
   - **Policy definition**: 
     ```sql
     (bucket_id = 'avatars')
     ```

## 🏃 Запуск приложения

### Режим разработки

```bash
pnpm dev
```

Приложение будет доступно по адресу: **http://localhost:3000**

### Сборка для production

```bash
# Сборка
pnpm build

# Запуск production сервера
pnpm start
```

### Проверка кода (линтинг)

```bash
pnpm lint
```

## 📁 Структура проекта

```
bunker-online-app/
├── app/                    # Next.js App Router
│   ├── api/               # API routes
│   ├── auth/              # Страницы аутентификации
│   ├── game/              # Игровые страницы
│   └── lobby/             # Лобби
├── components/            # React компоненты
│   ├── game/             # Игровые компоненты
│   └── ui/               # UI компоненты (shadcn/ui)
├── hooks/                # React хуки
├── lib/                  # Утилиты и конфигурация
│   └── supabase/         # Supabase клиенты
├── scripts/              # SQL скрипты
├── types/                # TypeScript типы
└── public/               # Статические файлы
```

## 🎮 Использование

### Первый запуск

1. Откройте http://localhost:3000
2. Нажмите "Регистрация"
3. Создайте аккаунт (email + пароль)
4. После регистрации вы автоматически войдете в систему
5. Создайте новую игру или присоединитесь по коду

### Создание игры

1. Нажмите "Создать игру" в лобби
2. Настройте параметры:
   - Количество игроков (8/12/16/20)
   - Время на раунд
   - Дополнительные настройки
3. Нажмите "Создать комнату"
4. Поделитесь кодом комнаты с друзьями

### Присоединение к игре

1. Нажмите "Присоединиться" в лобби
2. Введите код комнаты
3. Нажмите "Присоединиться"

## 🔧 Решение проблем

### Ошибка: "Failed to load game state"

**Причина**: Неправильно настроены переменные окружения или не создана база данных.

**Решение**:
1. Проверьте файл `.env.local`
2. Убедитесь, что SQL скрипт выполнен в Supabase
3. Проверьте, что ключи Supabase правильные

### Ошибка: "Not authenticated"

**Причина**: Проблемы с аутентификацией.

**Решение**:
1. Проверьте настройки Authentication в Supabase
2. Убедитесь, что Redirect URLs настроены правильно
3. Очистите cookies браузера

### Realtime не работает

**Причина**: Realtime не включен для таблиц.

**Решение**:
1. Перейдите в Database → Replication в Supabase
2. Включите репликацию для всех игровых таблиц

### Ошибка сборки TypeScript

**Причина**: Ошибки типов.

**Решение**:
```bash
# Проверьте ошибки
pnpm lint

# Или запустите с игнорированием ошибок (временно)
# В next.config.mjs уже включено ignoreBuildErrors: true
```

## 📚 Дополнительные ресурсы

- [Документация Next.js](https://nextjs.org/docs)
- [Документация Supabase](https://supabase.com/docs)
- [Документация shadcn/ui](https://ui.shadcn.com)
- [API Документация](./docs/API.md) - Полное описание всех API endpoints
- [Архитектура проекта](./АРХИТЕКТУРА.md) - Техническая архитектура
- [План разработки](./ПЛАН_РАЗРАБОТКИ.md) - Детальный план развития

## 🔐 Безопасность

⚠️ **Важно для production**:
- Никогда не коммитьте `.env.local` в git
- Используйте переменные окружения на сервере
- Настройте правильные RLS политики в Supabase
- Используйте service role key только на сервере (никогда в клиентском коде)

## 📝 Следующие шаги

После успешного запуска:
1. Протестируйте создание и присоединение к игре
2. Проверьте работу чата и голосования
3. Настройте WebRTC для видеосвязи (требует дополнительной настройки)
4. Настройте систему подписок (требует интеграции платежей)

## 🚀 Развертывание в Production

### Подготовка к развертыванию

1. **Настройте переменные окружения на сервере:**
   ```bash
   NEXT_PUBLIC_SUPABASE_URL=your_production_url
   NEXT_PUBLIC_SUPABASE_ANON_KEY=your_production_key
   ```

2. **Соберите приложение:**
   ```bash
   pnpm build
   ```

3. **Запустите production сервер:**
   ```bash
   pnpm start
   ```

### Рекомендации для Production

- Используйте переменные окружения вместо `.env.local`
- Настройте правильные CORS политики в Supabase
- Включите rate limiting на уровне сервера (Redis)
- Настройте мониторинг и логирование
- Используйте CDN для статических файлов
- Настройте резервное копирование базы данных

### Платформы для развертывания

- **Vercel** - Рекомендуется для Next.js приложений
- **Railway** - Простое развертывание с поддержкой PostgreSQL
- **DigitalOcean App Platform** - Гибкая платформа для Node.js
- **AWS / GCP / Azure** - Для enterprise решений

---

**Удачи в разработке! 🚀**
