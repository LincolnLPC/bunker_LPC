# 🔍 Диагностика проблемы с камерой и микрофоном

## Если разрешения предоставлены, но приложение не видит устройства

### Шаг 1: Откройте консоль браузера

1. Нажмите **F12** или **Ctrl+Shift+I** (Windows/Linux) / **Cmd+Option+I** (Mac)
2. Перейдите на вкладку **Console** (Консоль)
3. Обновите страницу игры

### Шаг 2: Найдите сообщения с префиксом `[Media]` или `[WebRTC]`

Вы должны увидеть следующие сообщения:

#### ✅ Если всё работает правильно:
```
[Media] Checking conditions for media initialization: {...}
[Media] Conditions met - requesting camera/microphone access...
[WebRTC] Checking available devices...
[WebRTC] Available devices: {videoDevices: X, audioDevices: Y, ...}
[WebRTC] Requesting camera and microphone access...
[WebRTC] ✅ Media access granted, stream obtained: {streamId: "...", videoTracks: 1, audioTracks: 1, ...}
[Media] Successfully initialized media stream: {...}
```

#### ⚠️ Если есть проблемы, вы увидите одно из следующих:

1. **`[Media] Conditions NOT met - waiting: ...`**
   - Это означает, что условия для запроса доступа не выполняются
   - Проверьте, какие условия не выполняются (например: "gameState still loading", "no roomId", и т.д.)

2. **`[WebRTC] Browser doesn't support getUserMedia`**
   - Браузер не поддерживает WebRTC API
   - Обновите браузер или используйте другой (Chrome, Firefox, Edge, Safari)

3. **`[WebRTC] Permission denied`**
   - Разрешения не предоставлены
   - Хотя вы говорите, что разрешили, браузер может блокировать
   - Попробуйте очистить разрешения и разрешить снова

4. **`[WebRTC] Available devices: {videoDevices: 0, audioDevices: 0}`**
   - Устройства не найдены системой
   - Проверьте, подключены ли камера и микрофон
   - Проверьте настройки Windows/macOS

5. **`[WebRTC] ⚠️ Video was requested but stream has no video tracks!`**
   - Stream получен, но треки пустые
   - Возможно, устройство используется другим приложением
   - Закройте другие приложения (Zoom, Teams, Skype, и т.д.)

6. **`[WebRTC] Error enumerating devices`**
   - Не удалось получить список устройств
   - Обычно это означает, что разрешения не предоставлены

### Шаг 3: Проверьте, используется ли устройство другим приложением

**Windows:**
1. Откройте Диспетчер задач (Ctrl+Shift+Esc)
2. Посмотрите, какие приложения используют камеру/микрофон
3. Закройте приложения, которые используют устройства (Zoom, Teams, Skype, Discord, и т.д.)

**macOS:**
1. Откройте Системные настройки → Конфиденциальность и безопасность → Камера/Микрофон
2. Проверьте, какие приложения имеют доступ
3. Закройте приложения, которые используют устройства

### Шаг 4: Проверьте разрешения в браузере

**Chrome/Edge:**
1. Нажмите на значок замка 🔒 в адресной строке (слева от URL)
2. Проверьте разрешения для камеры и микрофона
3. Если установлено "Заблокировано" или "Спросить", измените на "Разрешить"
4. Обновите страницу

**Firefox:**
1. Нажмите на значок замка в адресной строке
2. Нажмите "Больше информации"
3. Перейдите на вкладку "Разрешения"
4. Проверьте настройки камеры и микрофона

**Safari:**
1. Safari → Настройки → Веб-сайты → Камера/Микрофон
2. Найдите `localhost:3000` или ваш домен
3. Убедитесь, что установлено "Разрешить"

### Шаг 5: Попробуйте вручную запросить доступ

1. В игре нажмите кнопку **"Включить камеру"** (если она видна)
2. Это должно вызвать диалог браузера с запросом разрешения
3. Разрешите доступ
4. Проверьте консоль на наличие ошибок

### Шаг 6: Очистите разрешения и попробуйте снова

**Chrome/Edge:**
1. Откройте `chrome://settings/content/camera` (или `edge://settings/content/camera`)
2. Найдите `localhost:3000` или ваш домен
3. Удалите его из списка
4. Обновите страницу
5. При повторном запросе разрешите доступ

**Firefox:**
1. Откройте `about:preferences#privacy`
2. Найдите "Разрешения" → "Настройки"
3. Удалите разрешения для сайта
4. Обновите страницу

### Шаг 7: Проверьте, работает ли WebRTC в принципе

Выполните в консоли браузера:

```javascript
// Проверка поддержки
console.log("mediaDevices:", !!navigator.mediaDevices)
console.log("getUserMedia:", !!navigator.mediaDevices?.getUserMedia)

// Список устройств
navigator.mediaDevices.enumerateDevices().then(devices => {
  console.log("Доступные устройства:", devices.map(d => ({
    kind: d.kind,
    label: d.label || "Нет названия (нужно разрешение)",
    deviceId: d.deviceId
  })))
})

// Тест доступа
navigator.mediaDevices.getUserMedia({ video: true, audio: true })
  .then(stream => {
    console.log("✅ Доступ получен!", stream)
    console.log("Видео треки:", stream.getVideoTracks())
    console.log("Аудио треки:", stream.getAudioTracks())
    stream.getTracks().forEach(track => track.stop()) // Остановить после теста
  })
  .catch(err => {
    console.error("❌ Ошибка:", err.name, err.message)
  })
```

### Шаг 8: Что делать, если ничего не помогает

1. **Попробуйте другой браузер:**
   - Chrome (рекомендуется)
   - Firefox
   - Edge
   - Safari (только на macOS)

2. **Проверьте настройки Windows/macOS:**
   - Windows: Параметры → Конфиденциальность → Камера/Микрофон
   - macOS: Системные настройки → Конфиденциальность и безопасность → Камера/Микрофон

3. **Проверьте драйверы:**
   - Убедитесь, что драйверы камеры и микрофона установлены и обновлены
   - Попробуйте переподключить устройства

4. **Проверьте, работают ли устройства в других приложениях:**
   - Откройте приложение "Камера" (Windows) или Photo Booth (macOS)
   - Проверьте, работает ли камера
   - Проверьте микрофон в других приложениях (Zoom, Teams, и т.д.)

5. **Проверьте расширения браузера:**
   - Некоторые расширения могут блокировать доступ к камере/микрофону
   - Попробуйте отключить расширения или использовать режим инкогнито

---

## 📋 Скопируйте сообщения из консоли

Если проблема не решается, скопируйте все сообщения из консоли с префиксом `[Media]` и `[WebRTC]` и отправьте разработчику.

**Как скопировать:**
1. Откройте консоль (F12)
2. Правой кнопкой мыши на любом сообщении
3. Выберите "Сохранить как..." или скопируйте текст
4. Отправьте разработчику
