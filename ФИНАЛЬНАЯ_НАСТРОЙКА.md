# ✅ Почти готово! Остался последний шаг

## Что уже настроено:
- ✅ Файл `.env.local` с правильным anon public key
- ✅ Подключение к Supabase работает
- ✅ URL и ключи настроены корректно

## ⚠️ Последний шаг: Создание базы данных

Таблицы еще не созданы в вашей базе данных. Это нужно сделать один раз.

### Быстрый способ:

1. **Откройте SQL Editor в Supabase:**
   ```
   https://supabase.com/dashboard/project/pklwfuyxumtjbgyqlxsf/sql/new
   ```

2. **Скопируйте весь SQL код** из файла `scripts/001-create-tables.sql`

3. **Вставьте в SQL Editor** и нажмите **Run** (или Ctrl+Enter)

4. **Включите Realtime** (после создания таблиц):
   ```
   https://supabase.com/dashboard/project/pklwfuyxumtjbgyqlxsf/database/replication
   ```
   Включите репликацию для:
   - `game_rooms`
   - `game_players`
   - `chat_messages`
   - `player_characteristics`
   - `votes`

5. **Настройте Authentication** (если еще не настроено):
   ```
   https://supabase.com/dashboard/project/pklwfuyxumtjbgyqlxsf/auth/url-configuration
   ```
   Добавьте в Redirect URLs:
   - `http://localhost:3000/**`
   - `http://localhost:3000/auth/callback`

### Проверка после настройки:

Запустите:
```bash
node check-connection.js
```

Должно показать: ✅ **Подключение работает! Таблицы созданы.**

### Готово! 🎉

После этого перезапустите сервер:
```bash
pnpm dev
```

И откройте: http://localhost:3000

---

## Альтернатива: Автоматическое создание через Supabase CLI

Если у вас установлен Supabase CLI, можно выполнить:
```bash
supabase db push --db-url "postgresql://postgres:[PASSWORD]@db.pklwfuyxumtjbgyqlxsf.supabase.co:5432/postgres" < scripts/001-create-tables.sql
```

Но проще всего - через веб-интерфейс SQL Editor! 😊
